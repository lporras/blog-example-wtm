<li id="post-comment-<%= comment.id%>">
  <p><%=comment.message%></p>
  <% if user_signed_in? %>
    <%= link_to "Eliminar Comentario", post_comment_path(comment.post, comment), method: :delete, data: {confirm: "Estas seguro que deseas eliminar este articulo?", remote: true} %>
  <% end %>
</li>